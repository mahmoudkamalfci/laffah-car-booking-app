create a trip card component that will be used in profile/booking/page.tsx.
the trip card should be like the screenshot in prompts/trip-card.png.
the card should have box shadow shadow-(--primary-shadow) and rounded corners rounded-2xl and white background bg-white.
after you create the component import it in profile/booking/page.tsx and use it to display the trip card.
the trip card info should be passed as props to the component (object).
in profile/booking/page.tsx create array of objects to store the trip cards info.